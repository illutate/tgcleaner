firstly install reqs (requirements.txt, pip install - .txt)
and if u got any errors like C++ (run vs_BuildTools.exe and install only c++ build tools)
simply run start.py and set your api_id, api_hash, number, password, 2fa (if exist)
choose chats by numbers (ex.: 1,2,3,4 or 1) and enter the value for deleting msgs.
